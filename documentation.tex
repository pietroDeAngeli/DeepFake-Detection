\documentclass[a4paper,12pt]{article}

% Useful packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{graphicx}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{listingsutf8}
\usepackage{float}

% Title and Author
\title{DeepFake Detection - Documentation}
\author{Pietro De Angeli}

\lstset{
  breaklines=true,
  basicstyle=\ttfamily,
}

\begin{document}
\maketitle
\tableofcontents
%\newpage

\section{Introduction}

This project, part of the “Signal, Image and Video Processing” course, implements the paper \emph{"Efficient Temporally-Aware DeepFake Detection using H.264 Motion Vectors"}. The goal is to distinguish between DeepFake and real videos using \textbf{motion vectors} (MVs), a lightweight alternative to \textbf{Pptical flow} (OF).

MVs can be extracted directly from the H.264 video encoding (if supported), without further computation. Only motion vectors inside face regions are used, which are detected using the pretrained \textbf{YuNet} face detector (a lighter alternative to MTCNN used in the original paper).

The architecture consists of two identical MobileNetV3 networks, whose architecture has been slightly modified and where one process RGB images and the other processing MV+IM tensors. Their outputs are then combined to produce a final binary classification.

The paper criticizes traditional methods for only using per-frame predictions without considering temporal consistency. This solution leverages such temporal information by comparing how content moves across frames.

The paper uses MVs from H.264 as a low-cost approximation of optical flow (OF). Unlike OF, which operates at the pixel level, MVs describe how macroblocks (e.g., 16×16 pixels) move across frames. While less precise, MVs are significantly faster to obtain.

Their goal were to find a lighter version of optical flow that could have been used online and on low efficiency devices, however inference is efficient—bottlenecked mostly by face detection.

\section{Project Structure}

The project has the following directory structure:

\begin{verbatim}
|- dataset/
   |- video\_1/
   |- ...
   |- video\_N/
|FF++
|- models/
|- src/
   |- classifier/
   |- data\_preprocessing/
   |- tools/
   |- UI
|- test/
\end{verbatim}

% Fix from here
\subsection{\texttt{FF++}}
The Dataset I downloaded is \href{https://www.kaggle.com/datasets/hungle3401/faceforensics}{https://www.kaggle.com/datasets/hungle3401/faceforensics} which is a really small subset of the real one, I decided to pick this one just because it would have been much easier and less expensive to "train" it and implement a sort of Prove of Concept. 

The original dataset was consisted of two directories: 
\begin{itemize}
    \item \emph{real}: Contains original videos (200). 
    \item \emph{fake}: contains Deepfakes videos (200).  
\end{itemize}

\subsection{\texttt{dataset}}
The dataset described above has been preprocessed and structured in subdirectories, as described in the \emph{data\_preprocessing} section \ref{sec:data_preprocessing}. The result has been stored by saving a directory for each video:
\begin{itemize}
  \item \texttt{meta.json}: containing the original path of the video, the label that says if video is \emph{real} or \emph{fake} (with a value true or false), and the number of frames we've been able to extract. It's usually 100 but could be less. 
  \item \texttt{tensor.pt}: containing the stack of \texttt{motion\_vector\_x},\texttt{motion\_vector\_y},\texttt{information\_mask} for each frame.
  \item \texttt{faces}: in this directory there are the images of the faces detected in the frames.
\end{itemize}

There is also the file \texttt{manifest.json} containing 2 objects: train and test where each entry contains the name of the directory of the file and the label. The size of the two sets are 80\% for the train set and 20\% for the test set. 

The manifest file has been created by a file contained and described here \ref{sec:data_preprocessing}
% To here

\subsection{\texttt{models}}
\label{sec:models}

\begin{itemize}
  \item \textbf{YuNet}: a pretrained face detector returning face bounding boxes and confidence scores. This model is the bottleneck of our system. We use it to extract the faces from 100 random and unique frames of the video. 
  
  \item \textbf{Classifier}: \label{sec:model_changes}This model is based on \textbf{MobileNetV3 small} (downloaded with the pretrained weights) that takes both RGB and MV+IM features (two streams architecture) and returns the predictions of the video to be fake or not.  
  
  
  The paper suggests to modify the base version of the network by adding an \textbf{input} (3 channels for 224x224 images) and \textbf{output} (one classification value). In addition to the changes on the base model we also add a parameter that applies a weighted average to the results of the two streams; however, the averaging implemented by the paper does not contain any parameter.  
\end{itemize}

% fix from here
\subsection{\texttt{src}}

In this directory there is the \textit{Python} code of the project, it is organized in directories that I will describe below.

\subsubsection{\texttt{Classifier}}
% TODO
This directory are contains the processing applied to create the architecture of the \texttt{Classifier} described \ref{sec:model_changes} 

Here we have the following files:
\begin{itemize}
    \item \texttt{MobileNetV3.py}: which download the pretrained model and add the layers. 
    \item \texttt{network.py}: where the two streams architecture has been created.
    \item \texttt{training.py}: where we implement the training as described in Section \ref{sec:training}
    \item \texttt{evaluation.py}: where we test the accuracy of the network we trained.
\end{itemize}

\subsubsection{\texttt{Data\_preprocessing}}
\label{sec:data_preprocessing}
In this directory, is described the data pre-processing of the dataset and the splits of the training and test. 

In the file \texttt{create\_dataset.py} each video gets processed using the pipeline described in section \ref{sec:pipeline} and cerate the dataset as described in \ref{}
Then I used the function \texttt{split\_dataset()} to create the file \texttt{manifest.json} to create the train and test split (80-20\% respectively).

\subsubsection{\texttt{tools}}
\label{sec:tools}
Contains the implementation. In particular, the \texttt{tools} folder has:
\begin{itemize}
  \item \texttt{face\_detection.py}
  \item \texttt{motion\_vectors.py}
  \item \texttt{feature\_computation.py}
  \item \texttt{tools.py}
\end{itemize}

\subsection{Tools}
In this file there is the function \texttt{get\_dir\_videos()} that could be helpful in every file and returns a list of the paths of all the the .mp4 files contained in the directory specified in the parameter \texttt{path}. Here is also defined the function \texttt{pipeline()} that run the whole inference function.

I won't describe this section more deeply because I'll talk about this in the Pipeline section. 

\subsubsection{\texttt{UI}}
In this directory there is just a file, that defines a simple user interface structured as follow:
\begin{itemize}
    \item Upload Page: it has a Drag and drop option or a browse file to chose the video we want to analyze. By clicking on Detect we start the processing
    \item Progress Page: it's the page that make us wait the computation
    \item Result Page: it's the page that tells us the results: the time spent on each phase: face extraction, motion vector extraction and classification task; it will also show a button "go to the directory" to open the temp dir that will contain the 100 faces extracted and the time spent for each phase. 
\end{itemize}


\subsection{\texttt{test}}
In this directory there are the tests of the functions in \texttt{src} directory. The tests has been done using \texttt{pytest} and only the numeric output has been tested checking the following:
\begin{itemize}
    \item %TODO chatgpt, complete this part. 
\end{itemize}

\section{Pipeline}
\label{sec:pipeline}

The pipeline has described in the next sections. A complete version of the inference pipeline has been implemented in the file \texttt{tools.py} (\ref{sec:tools}) with the funciton \texttt{pipeline()}.
\subsection{Face Detection}

The paper suggests using MTCNN, but YuNet is used instead for efficiency. We define two classes:
\begin{itemize}
  \item \texttt{FaceBox}: holds a square bounding box (x, y, side).
  \item \texttt{Face}: holds a bounding box and the corresponding 224×224 BGR face image.
\end{itemize}

The function \texttt{initialize\_detector()} returns the YuNet model. \texttt{face\_frame\_extractor()} returns the largest face found, if any, resizing the crop to 224×224 and centering a square box.

Frame extraction is done via:
\begin{itemize}
  \item \texttt{random\_frame\_extractor()}: samples random timestamps, decoding a small window of frames and picking one.
  \item \texttt{unique\_frame\_extractor()}: uses reservoir sampling for uniform sampling over the whole video.
\end{itemize}

The wrapper \texttt{extract\_frames\_with\_faces()} chooses between the two, depending on the \texttt{unique\_frames} flag. These functions return up to 100 tuples of (frame, face), opening the video with motion vector support:
\begin{lstlisting}
stream.codec\_context.options = {"flags2": "+export\_mvs"}
\end{lstlisting}

\subsection{Motion Vector Extraction}

The class \texttt{MotionVector} holds:
\begin{itemize}
  \item \texttt{dst\_x}, \texttt{dst\_y}: current block location
  \item \texttt{motion\_x}, \texttt{motion\_y}: movement delta
  \item \texttt{motion\_scale}: scaling factor
\end{itemize}

The method \texttt{is\_in\_face()} checks whether a motion vector is inside a face region.

The function \texttt{extract\_motion\_vectors\_and\_im()} processes each frame-face pair and returns a list of tuples: (MV\_x, MV\_y, IM). It handles I-frames (all-zero), P/B-frames with valid motion vectors, and cases where MVs are missing.

Each motion vector is mapped to macroblocks, and we mark the intra-coded blocks via the \textbf{Information Mask (IM)}: 0 = inter-coded (has MV), 1 = intra-coded.

All outputs are resized to 224×224 using appropriate interpolation.

\subsection{Feature Computation}

The function \texttt{compute\_features\_video\_tensor()} stacks all per-frame features into a video tensor: shape (frames, height, width, 3).


To have the frame features we use the function \texttt{compute\_features\_frame()}:
\begin{itemize}
  \item Normalizes motion vectors over inter-coded regions (IM == 0)
  \item Stacks MV\_x, MV\_y, IM into a 3-channel feature map
\end{itemize}

% fix from here
\subsection{Classification}
Then the images extracted in the Face Detection phase and the tensors with the motion vectors are ready to fed the network. So the network is restored from the directory \texttt{models} \ref{sec:models}. Then all the input pass into the network and we have the result.
% To here

\section{Training the Model}
\label{sec:training}

The training has been done on 380 videos and tested on 80 and they are defined in the file \texttt{manifest.json}. The setup of the training has been done on 30 epochs but thanks to the early stopping implemented using K-fold only 18 has been completed. However the training is not relevant here because the training set is really small and we don't expect any results from that. However in the training phase we can notice the training error is linearly decreasing while the validation set is concanve, in particular gets the lower value at epoch 13, which is the epoch where the model is saved. 

\subsection{Augmentation}
As suggested in the paper the files in the train set has been augmented I kept a solution "on the fly" by applying the augmentations only at training time under a certain probability. I used the library \texttt{albumentations} to apply:
\begin{itemize}
    \item RGB distortion
    \item White noise
    ...% TODO
\end{itemize}

\subsection{Evaluation}
% TODO

\section{Tools \& Technologies}
In this section we will analyze the tools used and the tecnologies impegate in the project. 

\subsection{Conda}
To setup this project I created a \texttt{conda} envoiroment with \texttt{python==3.10} and a file \texttt{setup.py} to download automatically the dependencies by running \texttt{pip install .} from the main directory.

\subsection{Libraries}
The External libraries I used are:
\begin{itemize}
    \item \texttt{av}: (150.0.0) the library used to extract the motion vectors from the video.
    \item \texttt{albumentations}: it's used to apply the augmentation on the training and trying to mitigate the overfitting.
    \item \texttt{open-cv}: used for the image and video processing.
    \item \texttt{pytest}: used to do the tests 
    \item \texttt{torch}: used for the training of the network and as for the face detection tool. 
\end{itemize}


\subsection{RunPod}
Since the training on my laptop were slow I decided to use cloud platform to train it. I used a Pod on RunPod where using a \texttt{ssh} connection I uploaded the preprocessed dataset and the code from github, set the enviroment by the \texttt{setup.py} file and train the network. 

I used an image with Pytorch and Cuda pre-installed and a GPU a NVIDIA RTX4090.

\section{Results}
The results of the project were not to have a useful network but to implement the paper, implement the preprocessing for the videos and set a pipeline to train the network, of course to have a good final result I would need more resources. The goal od the project were not to train a model but to understand the preprocessing of the video without considering AI, and I can say I am proud of the job.

\section{Future works}
Since the final results are not really relevant a future work could be training the model on the whole FaceForentics dataset, using more computations on RunPod. Another problem of this project is YuNet which even if it's less expensive than MTCNN it's still the bottleneck of the project, so a even lighter model (or solution without AI) could be explored.


\end{document}
